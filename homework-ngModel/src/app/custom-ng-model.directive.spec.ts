import { CustomNgModelDirective } from './custom-ng-model.directive';

describe('CustomNgModelDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomNgModelDirective();
    expect(directive).toBeTruthy();
  });
});
